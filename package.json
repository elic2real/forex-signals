{
  "name": "forex-intelligence-suite",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r --filter ./packages... run build && pnpm -r --filter ./apps... run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "backend/*"
  ],
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "@types/react": "^19.1.16",
    "axios": "^1.12.2",
    "react": "^19.1.1",
    "tslib": "^2.8.1",
    "tsx": "^4.20.6",
    "vitest": "^1.6.1",
    "zod": "^3.25.76"
  }
}
