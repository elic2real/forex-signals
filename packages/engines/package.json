{
  "name": "@forex/engines",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p ../../tsconfig.base.json -d --outDir dist src",
    "typecheck": "tsc --noEmit -p ../../tsconfig.base.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "tslib": "^2.6.2"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "tsx": "^4.7.0"
  }
}
